import 'package:flutter/material.dart';

/// CareLink brand — production healthcare palette (light + dark screens).
class AppColors {
  static const Color primary = Color(0xFF0F766E);
  static const Color primaryDark = Color(0xFF0A5F5A);

  static const Color background = Color(0xFFE8F5F2);
  static const Color card = Colors.white;

  static const Color textDark = Color(0xFF1E1E1E);
  static const Color textLight = Color(0xFF6B7F88);

  static const Color star = Color(0xFFF6B63C);
  static const Color border = Color(0xFFE0E0E0);
}