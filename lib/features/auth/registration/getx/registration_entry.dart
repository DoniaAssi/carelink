import 'package:flutter/material.dart';

class CarelinkRegistrationEntry extends StatelessWidget {
  const CarelinkRegistrationEntry({super.key, this.initialRole});

  final Object? initialRole;

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Registration entry placeholder')));
  }
}
