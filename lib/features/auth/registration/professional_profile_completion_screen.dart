import 'package:flutter/material.dart';

class ProfessionalProfileCompletionScreen extends StatelessWidget {
  const ProfessionalProfileCompletionScreen({super.key, required this.user});

  final Object? user;

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Professional profile completion placeholder')));
  }
}
